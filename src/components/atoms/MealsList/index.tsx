import { Text } from "react-native";
import { Container, TitleList, } from "./style";

export function MealsList() {

    return (
        <Container>
            <TitleList>Refeições</TitleList>

        </Container>
    )
}